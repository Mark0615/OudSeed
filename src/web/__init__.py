"""FastAPI web app for self-serve onboarding (Phase 3).

Provides the product's own login (Google sign-in) and, in later slices, the
Meta/Google Ads OAuth connect flow. Built on the durable storage layer in
`src.storage`. See `docs/phase3_oauth_setup.md` for credential setup.
"""
